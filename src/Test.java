public class Test {
    /**
     * String convertir="";
     *     String convertir0 ="";
     *          for(int i = 0;i<motDePasse.length();i++){
     *         //  System.out.println("caractere mot de passe "+motDePasse.charAt(i)+" :"+(int)motDePasse.charAt(i));
     *         //Cette instruction permet le mot de passe sous forme de lettre alphabétique
     *         //convertir0 = convertir0+String.valueOf((char)((int)(motDePasse.charAt(i)+3)));
     *         //Cette instruction permet de convertir un mot de passe en chiffre
     *         motDePasseChiffer = motDePasseChiffer + String.valueOf(((int)motDePasse.charAt(i)+3));
     *     }
     *     //for (int i = 0;i<convertir.length();i++){System.out.println(convertir);}
     *     //System.out.println(convertir);
     *          System.out.println(motDePasseChiffer);
     */

}
